import { StatusChatDirective } from './status-chat.directive';

describe('StatusChatDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusChatDirective();
    expect(directive).toBeTruthy();
  });
});
